import { TopContainer, TopContent } from './components';

import NavigationBar from './NavigationBar';

const Top = () => {
  return (
    <TopContainer>
      <TopContent>
        <NavigationBar />
      </TopContent>
    </TopContainer>
  );
};

export default Top;
